<?xml version="1.0" encoding="utf-8"?>


<ScrollView
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.activity.FormularioAcitivity"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:fillViewport="true"
    >

  <androidx.constraintlayout.widget.ConstraintLayout
      android:layout_width="match_parent"
      android:layout_height="wrap_content">

    <EditText
        app:layout_constraintTop_toTopOf="parent"
        android:id="@+id/activity_formulario_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_margin="16dp"
        android:inputType="text"
        android:hint="Nome" />

    <EditText
        android:id="@+id/activity_formulario_descricao"
        app:layout_constraintTop_toBottomOf="@+id/activity_formulario_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/activity_formulario_name"
        app:layout_constraintEnd_toEndOf="@id/activity_formulario_name"
        android:hint="Descrição"
        android:inputType="text"
        />

    <EditText
        android:id="@+id/activity_formulario_valor"
        app:layout_constraintTop_toBottomOf="@+id/activity_formulario_descricao"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="@id/activity_formulario_name"
        app:layout_constraintEnd_toEndOf="@id/activity_formulario_name"
        android:hint="Valor"
        android:inputType="numberDecimal"
        />


    <Button
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintVertical_bias="1"
        app:layout_constraintTop_toBottomOf="@+id/activity_formulario_valor"
        android:text="Salvar"
        android:layout_margin="16dp"
        android:id="@+id/activity_formulario_butao_salvar"
        />

  </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>

